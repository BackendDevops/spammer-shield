@if($is_enabled)
    <input type="text" name="{{$input_name}}" class="{{$input_class}}" style="display: none;width: 0;height: 0">
@endif

@if($is_google_enabled)
@endif

@if($is_random_question_enabled)
@endif

@if($is_timeout_filter_enabled)
@endif
